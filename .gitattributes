# Keep shell/Make files LF-only so Windows checkouts do not break `make` / bash scripts
# with `$'\r': command not found`.
* text=auto eol=lf
*.sh text eol=lf
Makefile text eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
