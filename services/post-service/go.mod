module github.com/AnishK05/cascade-feed-ranking-engine/services/post-service

go 1.22.2

require google.golang.org/grpc v1.83.0

// The generated proto client/server stubs (github.com/.../proto/gen/go/...) are resolved to
// the local workspace copy via go.work at the repo root (see IMPLEMENTATION_PLAN.md §3) rather
// than a module proxy; that module is intentionally not listed as a direct `require` here.
